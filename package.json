{
  "name": "interactive-tale",
  "private": false,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "push-dir --dir=.output/public --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.11",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "autosize": "^5.0.1"
  }
}
